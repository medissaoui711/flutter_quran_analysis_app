import 'package:flutter/material.dart';

class AnalysisWidget extends StatelessWidget {
  const AnalysisWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to the Analysis Widget!'),
    );
  }
}
